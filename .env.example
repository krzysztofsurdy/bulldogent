# Secrets only — non-secret config lives in YAML files under config/
# Values use $(VAR) syntax in YAML to reference these env vars.
#
# Naming convention:
#   PLATFORM_<NAME>_*  — messaging platform credentials (Slack)
#   PROVIDER_<NAME>_*  — LLM provider credentials (OpenAI, Bedrock, Vertex)
#   TOOL_<NAME>_*      — tool integration credentials (Jira, Confluence, GitHub, Tavily)
#   EMBEDDING_*        — embedding provider credentials (shared across RAG subsystem)
#
# Individual tools use different key suffixes (api_key, token, api_token) because
# they match the terminology of each upstream API. This is intentional.

## Slack
PLATFORM_SLACK_BOT_TOKEN=xoxb-your-bot-token-here
PLATFORM_SLACK_APP_TOKEN=xapp-your-app-token-here


## OpenAI
PROVIDER_OPENAI_API_KEY=sk-xxx
# PROVIDER_OPENAI_API_URL=http://localhost:4000  # LiteLLM proxy, Ollama, etc.

## Bedrock
PROVIDER_BEDROCK_REGION=us-east-1
PROVIDER_BEDROCK_ANTHROPIC_VERSION=bedrock-2023-05-31
# PROVIDER_BEDROCK_API_URL=http://localhost:4566  # LocalStack endpoint

## Vertex
PROVIDER_VERTEX_PROJECT_ID=your-gcp-project-id
PROVIDER_VERTEX_LOCATION=us-central1
# PROVIDER_VERTEX_API_URL=  # custom Vertex endpoint

# Tools
## Jira
TOOL_JIRA_URL=https://your-domain.atlassian.net
TOOL_JIRA_USERNAME=your-email@example.com
TOOL_JIRA_API_TOKEN=your-jira-api-token

## Confluence
TOOL_CONFLUENCE_URL=https://your-domain.atlassian.net
TOOL_CONFLUENCE_USERNAME=your-email@example.com
TOOL_CONFLUENCE_API_TOKEN=your-confluence-api-token

## GitHub
TOOL_GITHUB_TOKEN=ghp_xxx

# Database (PostgreSQL + pgvector)
DATABASE_URL=postgresql+psycopg://bulldogent:bulldogent@localhost:5432/bulldogent

# Embedding (RAG)
EMBEDDING_API_KEY=sk-xxx                       # can reuse PROVIDER_OPENAI_API_KEY
# EMBEDDING_API_URL=                           # optional — LiteLLM, Ollama, etc.
# EMBEDDING_BEDROCK_REGION=us-east-1           # for Bedrock provider
# EMBEDDING_VERTEX_PROJECT_ID=your-project     # for Vertex provider
# EMBEDDING_VERTEX_LOCATION=us-central1        # for Vertex provider
